- args:
    sql: CREATE TABLE "users"("id" bigserial NOT NULL, "name" text NOT NULL, "type"
      text NOT NULL, "address" text NOT NULL, PRIMARY KEY ("id") )
  type: run_sql
- args:
    name: users
  type: add_existing_table_or_view
