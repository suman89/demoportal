- args:
    sql: CREATE TABLE "stores"("id" bigserial NOT NULL, "name" text NOT NULL, "address"
      text NOT NULL, "owner" bigint NOT NULL, PRIMARY KEY ("id") )
  type: run_sql
- args:
    name: stores
  type: add_existing_table_or_view
